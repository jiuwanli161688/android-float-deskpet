# 产品锁定

独立 Android 系统悬浮窗桌宠，不是全屏陪伴 App，也不依赖任何 PC / Tauri 桌宠工程。

## 已锁定

- 形态：`WindowManager` + `TYPE_APPLICATION_OVERLAY`，浮在微信 / 浏览器等其它应用之上。
- 交互：拖动、点击反应、双击表情、长按菜单（摸摸 / 喂食 / 睡觉）、甩出惯性、待机循环、松手回弹；可选穿透 / 幽灵模式。
- 自主：斜向闲逛、行走轻跳、停顿转向、弧线/两段路径、走向随机点后打盹；超时半藏探头（点按或摇一摇唤出）。
- 本地气泡：独立悬浮层，优先在角色上方，贴边时改到侧面或下方，不遮挡身体；按时段和心情、互动从词库里抽一句，不联网。
- 心情 / 亲密度 0–100，本地 SharedPreferences；喂食消耗本地零食计数并随时间回复。
- 三套换装帧：日常、睡衣、出行。短音效可静音。
- 数据：纯本地。无账号、无广告、无云、无 AI 对话。核心功能不声明 `INTERNET`。
- 系统：仅 Android。`minSdk 26`，`targetSdk 34`，`applicationId = com.floatdeskpet.app`。
- 角色：二次元 Q 版成年少女「杏杏」，健康向，非写实抠图。待机 + 点击帧动画。
- 运行：前台服务 + 常驻通知（运行中 / 显隐 / 退出）。退出时卸窗并停服务。
- 设置页：Material3 中文；大小、透明度、换装、静音、始终显示、穿透、启停、国产 ROM 省电说明、隐私说明。
- 1.2 轻接入：半藏时摇一摇唤醒；低电量 / 充电气泡（BatteryManager）；长按喂食扣本地计数。

## 明确不做（v1 / v1.1）

Live2D Cubism、iOS、AI 聊天、商店上架材料、无障碍 App 过滤、多只桌宠刷屏、任何服务端。
